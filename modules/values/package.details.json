{
  "template":    "springboot",
  "name":        "values",
  "description": "id / value / identity",
  "mvn":         {
    "project": {
      "dependencies": {
        "dependency": [
          {"groupId": "${group}", "artifactId": "utils", "version":  "${version}"}
        ]
      }
    }
  },
  "links":       ["utils"],
  "guards":      {
    "mvn":     true,
    "compile": true,
    "publish": false,
    "test":    true
  }
}